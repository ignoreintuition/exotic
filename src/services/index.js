const speciesService = require('./species');
const petsService = require('./pets');
const usersService = require('./users');

module.exports = {
    speciesService,
    petsService,
    usersService
}

